# pi-web-search

Web search and URL content grep for pi.

## Usage

**Search:** `action: "search"`, `input: "query"`  
**Grep URL content:** `url: "https://url"`, `query: "search term"`

## Requirements

- SearXNG at http://127.0.0.1:8080 (set `SEARXNG_URL` to change)
